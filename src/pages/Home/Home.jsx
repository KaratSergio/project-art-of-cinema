import { MovieList } from '../../components/MovieList/MovieList';

export const Home = () => {
  return (
    <section>
      <div>
        <MovieList />
      </div>
    </section>
  );
};

export default Home;
